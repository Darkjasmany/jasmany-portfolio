---
import { config } from "../../data/config";
import { bioData } from "../../data/bioData";
import Title from "../ui/Title.astro";
---

<section class="w-full overflow-hidden transition-colors duration-300">
  <div
    class="z-10 w-full max-w-7xl mx-auto px-4 sm:px-5 md:px-6 lg:px-10 pt-24 md:pt-26 lg:pt-28 pb-10 md:pb-16"
  >
    <div class="flex flex-col">
      <div class="flex-1 w-full min-w-0">
        <Title title="Sobre " span="Mí" />
        <div class="my-6 space-y-5">
          {
            bioData.paragraphs.map((p) => (
              <p class="text-base md:text-lg leading-7 md:leading-8 text-[var(--hero-desc)]" set:html={p} />
            ))
          }
        </div>
        <div class="mt-8 flex items-center gap-4">
          <a
            href={config.curriculumUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 px-5 py-2.5 rounded-xl text-sm font-semibold bg-[var(--accent)] text-white hover:brightness-110 hover:shadow-lg hover:shadow-[var(--accent)]/25 active:scale-95 transition-all"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2.5"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
              <polyline points="7 10 12 15 17 10"></polyline>
              <line x1="12" y1="15" x2="12" y2="3"></line>
            </svg>
            Descargar CV
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
